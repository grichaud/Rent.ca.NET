# .NET
bin/
obj/
out/
publish/
*.user
*.suo
.vs/

# Rider / VS Code
.idea/
.vscode/

# NuGet
*.nupkg
packages/

# EF Core
*.mdf
*.ldf

# Node / Tailwind
node_modules/
src/Rent.Web/wwwroot/css/site.css
src/Rent.Web/wwwroot/css/site.css.map

# User uploads (dev only; prod usa Azure Blob)
src/Rent.Web/wwwroot/uploads/

# OS / editor
Thumbs.db
.DS_Store
*.swp

# Secrets
**/appsettings.Local.json
.env
.env.*
!.env.example

# Local-only test artefacts
/.playwright-mcp/
/slice*-*.png
# Screenshots sueltos en la raiz (parity/azure/home/etc.) — no ensuciar git status
/*.png

# Claude Code runtime lock (ephemeral, machine-local — never commit)
.claude/scheduled_tasks.lock

# ── La fabrica de agentes NO se publica (decision de Gio, 2026-08-17) ──
# El codigo de los agentes es privado; lo que se publica es la aplicacion.
# Estas rutas se purgaron del historial completo con git-filter-repo ese dia.
.claude/
CLAUDE.md
AGENTS.md
**/AGENTS.md
.mcp.json
BASELINE.md
ESTADO.md
REPORTE-FABRICA-*.md
